{
  "name": "mailbun",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first email client — multi-account IMAP/SMTP, unified inbox, threading, search.",
  "author": "Mailbun",
  "main": "electron/dist/main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/magnus530/mailbun.git"
  },
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:client",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w server && npm run typecheck -w client",
    "start": "node server/dist/index.js",
    "electron:compile": "tsc -p electron/tsconfig.json",
    "electron:dev": "npm run electron:compile && run-p -r dev electron:dev:window",
    "electron:dev:window": "wait-on tcp:127.0.0.1:5173 && electron electron/dist/main.js",
    "server:bundle": "esbuild server/dist/server.js --bundle --platform=node --format=cjs --external:better-sqlite3 --outfile=server/bundle/server.cjs",
    "electron:build": "npm run build && npm run server:bundle && npm run electron:compile && electron-builder"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "electron-updater": "^6.3.9"
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "esbuild": "^0.24.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.6.0",
    "wait-on": "^8.0.1"
  }
}
